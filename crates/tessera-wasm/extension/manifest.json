{
  "manifest_version": 3,
  "name": "Tessera Presentation (scaffold)",
  "version": "0.0.1",
  "description": "Attaches a Tessera-Presentation ARC header to outgoing requests. SCAFFOLD — NOT per-request unlinkable (declarativeNetRequest reuses one value across requests; use the tessera-client proxy for real unlinkability). See README.md.",
  "permissions": [
    "declarativeNetRequest",
    "declarativeNetRequestFeedback",
    "storage",
    "alarms"
  ],
  "host_permissions": [
    "http://127.0.0.1:8090/*"
  ],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "content_security_policy": {
    "extension_pages": "script-src 'self' 'wasm-unsafe-eval'; object-src 'self'"
  },
  "minimum_chrome_version": "121"
}
